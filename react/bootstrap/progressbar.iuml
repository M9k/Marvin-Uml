class ProgressBar{
    --props--
        + min(number)
        + now(number)
        + max(number)
        + label(node)
        + srOnly(bool)
        + striped(bool)
        + active(bool)
        + children(onlyProgressBar)
        + isChild(bool)
        + bsClass(string)
}